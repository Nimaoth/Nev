# This file is auto generated by tools/command_generator.nim

import std/[options, json, tables]
import misc/[jsonex]
import scripting_api


proc lspLogVerboseWrapper(args: string): string {.gcsafe.} =
  {.gcsafe.}:
    try:
      let args {.used.} = args.parseJsonexArgs()
      lspLogVerbose(getArg[bool](args.unnamed, args.named, 0, "val"))
      return ""
    except CatchableError:
      return "Failed to execute command lsp.lsp-log-verbose: " & getCurrentExceptionMsg() & " " & getCurrentException().getStackTrace()

proc lspToggleLogServerDebugWrapper(args: string): string {.gcsafe.} =
  {.gcsafe.}:
    try:
      let args {.used.} = args.parseJsonexArgs()
      lspToggleLogServerDebug()
      return ""
    except CatchableError:
      return "Failed to execute command lsp.lsp-toggle-log-server-debug: " & getCurrentExceptionMsg() & " " & getCurrentException().getStackTrace()

proc lspLogServerDebugWrapper(args: string): string {.gcsafe.} =
  {.gcsafe.}:
    try:
      let args {.used.} = args.parseJsonexArgs()
      lspLogServerDebug(getArg[bool](args.unnamed, args.named, 0, "val"))
      return ""
    except CatchableError:
      return "Failed to execute command lsp.lsp-log-server-debug: " & getCurrentExceptionMsg() & " " & getCurrentException().getStackTrace()

proc listLanguageServersWrapper(args: string): string {.gcsafe.} =
  ## List all language servers (excluding builtins)
  {.gcsafe.}:
    try:
      let args {.used.} = args.parseJsonexArgs()
      let self: LanguageServerLspService = getServiceChecked(LanguageServerLspService)
      listLanguageServers(self)
      return ""
    except CatchableError:
      return "Failed to execute command lsp.list: " & getCurrentExceptionMsg() & " " & getCurrentException().getStackTrace()

proc registerCommands(commands: CommandService) =
  const namespace = "lsp"
  discard commands.registerCommand(command_service.Command(
    namespace: namespace, name: "lsp-log-verbose", execute: lspLogVerboseWrapper, active: false,))
  discard commands.registerCommand(command_service.Command(
    namespace: namespace, name: "lsp-toggle-log-server-debug", execute: lspToggleLogServerDebugWrapper, active: false,))
  discard commands.registerCommand(command_service.Command(
    namespace: namespace, name: "lsp-log-server-debug", execute: lspLogServerDebugWrapper, active: false,))
  discard commands.registerCommand(command_service.Command(
    namespace: namespace, name: "list", execute: listLanguageServersWrapper, active: false,
    description: """List all language servers (excluding builtins)""",))
