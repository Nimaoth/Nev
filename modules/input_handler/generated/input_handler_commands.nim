# This file is auto generated by tools/command_generator.nim

import std/[options, json, tables]
import misc/[jsonex]
import scripting_api


proc setLeaderWrapper(args: string): string {.gcsafe.} =
  {.gcsafe.}:
    try:
      let args {.used.} = args.parseJsonexArgs()
      let self: EventHandlerService = getServiceChecked(EventHandlerService)
      setLeader(self, getArg[string](args.unnamed, args.named, 0, "leader"))
      return ""
    except CatchableError:
      return "Failed to execute command events.set-leader: " & getCurrentExceptionMsg() & " " & getCurrentException().getStackTrace()

proc setLeadersWrapper(args: string): string {.gcsafe.} =
  {.gcsafe.}:
    try:
      let args {.used.} = args.parseJsonexArgs()
      let self: EventHandlerService = getServiceChecked(EventHandlerService)
      setLeaders(self, getArg[seq[string]](args.unnamed, args.named, 0, "leaders"))
      return ""
    except CatchableError:
      return "Failed to execute command events.set-leaders: " & getCurrentExceptionMsg() & " " & getCurrentException().getStackTrace()

proc addLeaderWrapper(args: string): string {.gcsafe.} =
  {.gcsafe.}:
    try:
      let args {.used.} = args.parseJsonexArgs()
      let self: EventHandlerService = getServiceChecked(EventHandlerService)
      addLeader(self, getArg[string](args.unnamed, args.named, 0, "leader"))
      return ""
    except CatchableError:
      return "Failed to execute command events.add-leader: " & getCurrentExceptionMsg() & " " & getCurrentException().getStackTrace()

proc addLeadersWrapper(args: string): string {.gcsafe.} =
  {.gcsafe.}:
    try:
      let args {.used.} = args.parseJsonexArgs()
      let self: EventHandlerService = getServiceChecked(EventHandlerService)
      addLeaders(self, getArg[seq[string]](args.unnamed, args.named, 0, "leaders"))
      return ""
    except CatchableError:
      return "Failed to execute command events.add-leaders: " & getCurrentExceptionMsg() & " " & getCurrentException().getStackTrace()

proc clearCommandsWrapper(args: string): string {.gcsafe.} =
  {.gcsafe.}:
    try:
      let args {.used.} = args.parseJsonexArgs()
      let self: EventHandlerService = getServiceChecked(EventHandlerService)
      clearCommands(self, getArg[string](args.unnamed, args.named, 0, "context"))
      return ""
    except CatchableError:
      return "Failed to execute command events.clear-commands: " & getCurrentExceptionMsg() & " " & getCurrentException().getStackTrace()

proc registerCommands(commands: CommandService) =
  const namespace = "events"
  discard commands.registerCommand(command_service.Command(
    namespace: namespace, name: "set-leader", execute: setLeaderWrapper, active: false,))
  discard commands.registerCommand(command_service.Command(
    namespace: namespace, name: "set-leaders", execute: setLeadersWrapper, active: false,))
  discard commands.registerCommand(command_service.Command(
    namespace: namespace, name: "add-leader", execute: addLeaderWrapper, active: false,))
  discard commands.registerCommand(command_service.Command(
    namespace: namespace, name: "add-leaders", execute: addLeadersWrapper, active: false,))
  discard commands.registerCommand(command_service.Command(
    namespace: namespace, name: "clear-commands", execute: clearCommandsWrapper, active: false,))
