# This file is auto generated by tools/command_generator.nim

import std/[options, json, tables]
import misc/[jsonex]
import scripting_api


proc undoTreeToggleWrapper(args: string): string {.gcsafe.} =
  ## Show undo tree for current buffer
  {.gcsafe.}:
    try:
      let args {.used.} = args.parseJsonexArgs()
      let view: UndoTreeView = getUndoTreeView()
      undoTreeToggle(view)
      return ""
    except CatchableError:
      return "Failed to execute command undotree.toggle: " & getCurrentExceptionMsg() & " " & getCurrentException().getStackTrace()

proc undoTreeToggleAutoApplyWrapper(args: string): string {.gcsafe.} =
  ## Toggle the auto apply setting
  {.gcsafe.}:
    try:
      let args {.used.} = args.parseJsonexArgs()
      let view: UndoTreeView = getUndoTreeView()
      undoTreeToggleAutoApply(view)
      return ""
    except CatchableError:
      return "Failed to execute command undotree.toggle-auto-apply: " & getCurrentExceptionMsg() & " " & getCurrentException().getStackTrace()

proc undoTreePrevChangeWrapper(args: string): string {.gcsafe.} =
  ## Go to previous change in undo tree
  {.gcsafe.}:
    try:
      let args {.used.} = args.parseJsonexArgs()
      let view: UndoTreeView = getUndoTreeView()
      undoTreePrevChange(view)
      return ""
    except CatchableError:
      return "Failed to execute command undotree.prev-change: " & getCurrentExceptionMsg() & " " & getCurrentException().getStackTrace()

proc undoTreeNextChangeWrapper(args: string): string {.gcsafe.} =
  ## Go to next change in undo tree
  {.gcsafe.}:
    try:
      let args {.used.} = args.parseJsonexArgs()
      let view: UndoTreeView = getUndoTreeView()
      undoTreeNextChange(view)
      return ""
    except CatchableError:
      return "Failed to execute command undotree.next-change: " & getCurrentExceptionMsg() & " " & getCurrentException().getStackTrace()

proc undoTreePrevChangeTimeWrapper(args: string): string {.gcsafe.} =
  ## Go to previous change by stepping by a certain time interval in undo tree
  {.gcsafe.}:
    try:
      let args {.used.} = args.parseJsonexArgs()
      let view: UndoTreeView = getUndoTreeView()
      undoTreePrevChangeTime(view, getArg[string](args.unnamed, args.named, 0, "args", ""))
      return ""
    except CatchableError:
      return "Failed to execute command undotree.prev-change-time: " & getCurrentExceptionMsg() & " " & getCurrentException().getStackTrace()

proc undoTreeNextChangeTimeWrapper(args: string): string {.gcsafe.} =
  ## Go to next change by stepping by a certain time interval in undo tree
  {.gcsafe.}:
    try:
      let args {.used.} = args.parseJsonexArgs()
      let view: UndoTreeView = getUndoTreeView()
      undoTreeNextChangeTime(view, getArg[string](args.unnamed, args.named, 0, "args", ""))
      return ""
    except CatchableError:
      return "Failed to execute command undotree.next-change-time: " & getCurrentExceptionMsg() & " " & getCurrentException().getStackTrace()

proc undoTreeFirstChangeWrapper(args: string): string {.gcsafe.} =
  ## Go to first change in undo tree
  {.gcsafe.}:
    try:
      let args {.used.} = args.parseJsonexArgs()
      let view: UndoTreeView = getUndoTreeView()
      undoTreeFirstChange(view)
      return ""
    except CatchableError:
      return "Failed to execute command undotree.first-change: " & getCurrentExceptionMsg() & " " & getCurrentException().getStackTrace()

proc undoTreeLastChangeWrapper(args: string): string {.gcsafe.} =
  ## Go to last change in undo tree
  {.gcsafe.}:
    try:
      let args {.used.} = args.parseJsonexArgs()
      let view: UndoTreeView = getUndoTreeView()
      undoTreeLastChange(view)
      return ""
    except CatchableError:
      return "Failed to execute command undotree.last-change: " & getCurrentExceptionMsg() & " " & getCurrentException().getStackTrace()

proc undoTreeLeftChangeWrapper(args: string): string {.gcsafe.} =
  ## Go to next change on the left branch in the undo tree
  {.gcsafe.}:
    try:
      let args {.used.} = args.parseJsonexArgs()
      let view: UndoTreeView = getUndoTreeView()
      undoTreeLeftChange(view)
      return ""
    except CatchableError:
      return "Failed to execute command undotree.left-change: " & getCurrentExceptionMsg() & " " & getCurrentException().getStackTrace()

proc undoTreeRightChangeWrapper(args: string): string {.gcsafe.} =
  ## Go to next change on the right branch in the undo tree
  {.gcsafe.}:
    try:
      let args {.used.} = args.parseJsonexArgs()
      let view: UndoTreeView = getUndoTreeView()
      undoTreeRightChange(view)
      return ""
    except CatchableError:
      return "Failed to execute command undotree.right-change: " & getCurrentExceptionMsg() & " " & getCurrentException().getStackTrace()

proc undoTreeActiveChildWrapper(args: string): string {.gcsafe.} =
  ## Go to the active child of the current change in the undo tree
  {.gcsafe.}:
    try:
      let args {.used.} = args.parseJsonexArgs()
      let view: UndoTreeView = getUndoTreeView()
      undoTreeActiveChild(view)
      return ""
    except CatchableError:
      return "Failed to execute command undotree.active-child: " & getCurrentExceptionMsg() & " " & getCurrentException().getStackTrace()

proc undoTreeParentChangeWrapper(args: string): string {.gcsafe.} =
  ## Go to parent of the current change in the undo tree
  {.gcsafe.}:
    try:
      let args {.used.} = args.parseJsonexArgs()
      let view: UndoTreeView = getUndoTreeView()
      undoTreeParentChange(view)
      return ""
    except CatchableError:
      return "Failed to execute command undotree.parent-change: " & getCurrentExceptionMsg() & " " & getCurrentException().getStackTrace()

proc undoTreeSelectCurrentWrapper(args: string): string {.gcsafe.} =
  ## Go to current change in undo tree
  {.gcsafe.}:
    try:
      let args {.used.} = args.parseJsonexArgs()
      let view: UndoTreeView = getUndoTreeView()
      undoTreeSelectCurrent(view)
      return ""
    except CatchableError:
      return "Failed to execute command undotree.select-current: " & getCurrentExceptionMsg() & " " & getCurrentException().getStackTrace()

proc undoTreeApplySelectedWrapper(args: string): string {.gcsafe.} =
  ## Make the selected change the current one.
  {.gcsafe.}:
    try:
      let args {.used.} = args.parseJsonexArgs()
      let view: UndoTreeView = getUndoTreeView()
      undoTreeApplySelected(view)
      return ""
    except CatchableError:
      return "Failed to execute command undotree.apply-selected: " & getCurrentExceptionMsg() & " " & getCurrentException().getStackTrace()

proc registerCommands(commands: CommandService) =
  const namespace = "undotree"
  discard commands.registerCommand(command_service.Command(
    namespace: namespace, name: "toggle", execute: undoTreeToggleWrapper, active: false,
    description: """Show undo tree for current buffer""",))
  discard commands.registerCommand(command_service.Command(
    namespace: namespace, name: "toggle-auto-apply", execute: undoTreeToggleAutoApplyWrapper, active: false,
    description: """Toggle the auto apply setting""",))
  discard commands.registerCommand(command_service.Command(
    namespace: namespace, name: "prev-change", execute: undoTreePrevChangeWrapper, active: false,
    description: """Go to previous change in undo tree""",))
  discard commands.registerCommand(command_service.Command(
    namespace: namespace, name: "next-change", execute: undoTreeNextChangeWrapper, active: false,
    description: """Go to next change in undo tree""",))
  discard commands.registerCommand(command_service.Command(
    namespace: namespace, name: "prev-change-time", execute: undoTreePrevChangeTimeWrapper, active: false,
    description: """Go to previous change by stepping by a certain time interval in undo tree""",))
  discard commands.registerCommand(command_service.Command(
    namespace: namespace, name: "next-change-time", execute: undoTreeNextChangeTimeWrapper, active: false,
    description: """Go to next change by stepping by a certain time interval in undo tree""",))
  discard commands.registerCommand(command_service.Command(
    namespace: namespace, name: "first-change", execute: undoTreeFirstChangeWrapper, active: false,
    description: """Go to first change in undo tree""",))
  discard commands.registerCommand(command_service.Command(
    namespace: namespace, name: "last-change", execute: undoTreeLastChangeWrapper, active: false,
    description: """Go to last change in undo tree""",))
  discard commands.registerCommand(command_service.Command(
    namespace: namespace, name: "left-change", execute: undoTreeLeftChangeWrapper, active: false,
    description: """Go to next change on the left branch in the undo tree""",))
  discard commands.registerCommand(command_service.Command(
    namespace: namespace, name: "right-change", execute: undoTreeRightChangeWrapper, active: false,
    description: """Go to next change on the right branch in the undo tree""",))
  discard commands.registerCommand(command_service.Command(
    namespace: namespace, name: "active-child", execute: undoTreeActiveChildWrapper, active: false,
    description: """Go to the active child of the current change in the undo tree""",))
  discard commands.registerCommand(command_service.Command(
    namespace: namespace, name: "parent-change", execute: undoTreeParentChangeWrapper, active: false,
    description: """Go to parent of the current change in the undo tree""",))
  discard commands.registerCommand(command_service.Command(
    namespace: namespace, name: "select-current", execute: undoTreeSelectCurrentWrapper, active: false,
    description: """Go to current change in undo tree""",))
  discard commands.registerCommand(command_service.Command(
    namespace: namespace, name: "apply-selected", execute: undoTreeApplySelectedWrapper, active: false,
    description: """Make the selected change the current one.""",))
