# This file is auto generated by tools/command_generator.nim

import std/[options, json, tables]
import misc/[jsonex]
import scripting_api


proc lspServerStartWrapper(args: string): string {.gcsafe.} =
  ## Start LSP server, reading from stdin
  {.gcsafe.}:
    try:
      let args {.used.} = args.parseJsonexArgs()
      let self: LspServerService = getServiceChecked(LspServerService)
      lspServerStart(self)
      return ""
    except CatchableError:
      return "Failed to execute command lsp-server.start: " & getCurrentExceptionMsg() & " " & getCurrentException().getStackTrace()

proc registerCommands(commands: CommandService) =
  const namespace = "lsp-server"
  discard commands.registerCommand(command_service.Command(
    namespace: namespace, name: "start", execute: lspServerStartWrapper, active: false,
    description: """Start LSP server, reading from stdin""",))
