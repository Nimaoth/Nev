# This file is auto generated by tools/command_generator.nim

import std/[options, json, tables]
import misc/[jsonex]
import scripting_api


proc gitUiToggleWrapper(args: string): string {.gcsafe.} =
  ## Toggle git ui
  {.gcsafe.}:
    try:
      let args {.used.} = args.parseJsonexArgs()
      let view: GitUiView = getGitUiView()
      gitUiToggle(view)
      return ""
    except CatchableError:
      return "Failed to execute command gitui.toggle: " & getCurrentExceptionMsg() & " " & getCurrentException().getStackTrace()

proc gitUiCursorDownWrapper(args: string): string {.gcsafe.} =
  ## Move cursor down
  {.gcsafe.}:
    try:
      let args {.used.} = args.parseJsonexArgs()
      let view: GitUiView = getGitUiView()
      gitUiCursorDown(view)
      return ""
    except CatchableError:
      return "Failed to execute command gitui.cursor-down: " & getCurrentExceptionMsg() & " " & getCurrentException().getStackTrace()

proc gitUiCursorUpWrapper(args: string): string {.gcsafe.} =
  ## Move cursor up
  {.gcsafe.}:
    try:
      let args {.used.} = args.parseJsonexArgs()
      let view: GitUiView = getGitUiView()
      gitUiCursorUp(view)
      return ""
    except CatchableError:
      return "Failed to execute command gitui.cursor-up: " & getCurrentExceptionMsg() & " " & getCurrentException().getStackTrace()

proc gitUiPushWrapper(args: string): string {.gcsafe.} =
  ## Push
  {.gcsafe.}:
    try:
      let args {.used.} = args.parseJsonexArgs()
      let view: GitUiView = getGitUiView()
      gitUiPush(view)
      return ""
    except CatchableError:
      return "Failed to execute command gitui.push: " & getCurrentExceptionMsg() & " " & getCurrentException().getStackTrace()

proc gitUiPullWrapper(args: string): string {.gcsafe.} =
  ## Pull
  {.gcsafe.}:
    try:
      let args {.used.} = args.parseJsonexArgs()
      let view: GitUiView = getGitUiView()
      gitUiPull(view)
      return ""
    except CatchableError:
      return "Failed to execute command gitui.pull: " & getCurrentExceptionMsg() & " " & getCurrentException().getStackTrace()

proc gitUiStashWrapper(args: string): string {.gcsafe.} =
  ## Stash changes
  {.gcsafe.}:
    try:
      let args {.used.} = args.parseJsonexArgs()
      let view: GitUiView = getGitUiView()
      gitUiStash(view)
      return ""
    except CatchableError:
      return "Failed to execute command gitui.stash: " & getCurrentExceptionMsg() & " " & getCurrentException().getStackTrace()

proc gitUiStashPopWrapper(args: string): string {.gcsafe.} =
  ## Pop latest stash
  {.gcsafe.}:
    try:
      let args {.used.} = args.parseJsonexArgs()
      let view: GitUiView = getGitUiView()
      gitUiStashPop(view)
      return ""
    except CatchableError:
      return "Failed to execute command gitui.stash-pop: " & getCurrentExceptionMsg() & " " & getCurrentException().getStackTrace()

proc gitUiCommitWrapper(args: string): string {.gcsafe.} =
  ## Commit
  {.gcsafe.}:
    try:
      let args {.used.} = args.parseJsonexArgs()
      let view: GitUiView = getGitUiView()
      gitUiCommit(view)
      return ""
    except CatchableError:
      return "Failed to execute command gitui.commit: " & getCurrentExceptionMsg() & " " & getCurrentException().getStackTrace()

proc gitUiCommitAmendWrapper(args: string): string {.gcsafe.} =
  ## Amend latest commit
  {.gcsafe.}:
    try:
      let args {.used.} = args.parseJsonexArgs()
      let view: GitUiView = getGitUiView()
      gitUiCommitAmend(view)
      return ""
    except CatchableError:
      return "Failed to execute command gitui.commit-amend: " & getCurrentExceptionMsg() & " " & getCurrentException().getStackTrace()

proc gitUiCommitEditStartWrapper(args: string): string {.gcsafe.} =
  ## Start editing commit message
  {.gcsafe.}:
    try:
      let args {.used.} = args.parseJsonexArgs()
      let view: GitUiView = getGitUiView()
      gitUiCommitEditStart(view)
      return ""
    except CatchableError:
      return "Failed to execute command gitui.commit-edit-start: " & getCurrentExceptionMsg() & " " & getCurrentException().getStackTrace()

proc gitUiCommitEditCancelWrapper(args: string): string {.gcsafe.} =
  ## Cancel editing commit message
  {.gcsafe.}:
    try:
      let args {.used.} = args.parseJsonexArgs()
      let view: GitUiView = getGitUiView()
      gitUiCommitEditCancel(view)
      return ""
    except CatchableError:
      return "Failed to execute command gitui.commit-edit-cancel: " & getCurrentExceptionMsg() & " " & getCurrentException().getStackTrace()

proc gitUiCommitEditConfirmWrapper(args: string): string {.gcsafe.} =
  ## Confirm commit message
  {.gcsafe.}:
    try:
      let args {.used.} = args.parseJsonexArgs()
      let view: GitUiView = getGitUiView()
      gitUiCommitEditConfirm(view)
      return ""
    except CatchableError:
      return "Failed to execute command gitui.commit-edit-confirm: " & getCurrentExceptionMsg() & " " & getCurrentException().getStackTrace()

proc gitUiSwitchBranchWrapper(args: string): string {.gcsafe.} =
  ## Switch branch
  {.gcsafe.}:
    try:
      let args {.used.} = args.parseJsonexArgs()
      let view: GitUiView = getGitUiView()
      gitUiSwitchBranch(view)
      return ""
    except CatchableError:
      return "Failed to execute command gitui.switch-branch: " & getCurrentExceptionMsg() & " " & getCurrentException().getStackTrace()

proc gitUiFetchWrapper(args: string): string {.gcsafe.} =
  ## Fetch
  {.gcsafe.}:
    try:
      let args {.used.} = args.parseJsonexArgs()
      let view: GitUiView = getGitUiView()
      gitUiFetch(view)
      return ""
    except CatchableError:
      return "Failed to execute command gitui.fetch: " & getCurrentExceptionMsg() & " " & getCurrentException().getStackTrace()

proc gitUiResetSoftWrapper(args: string): string {.gcsafe.} =
  ## Soft reset
  {.gcsafe.}:
    try:
      let args {.used.} = args.parseJsonexArgs()
      let view: GitUiView = getGitUiView()
      gitUiResetSoft(view)
      return ""
    except CatchableError:
      return "Failed to execute command gitui.reset-soft: " & getCurrentExceptionMsg() & " " & getCurrentException().getStackTrace()

proc gitUiStageAllWrapper(args: string): string {.gcsafe.} =
  ## Stage all files
  {.gcsafe.}:
    try:
      let args {.used.} = args.parseJsonexArgs()
      let view: GitUiView = getGitUiView()
      gitUiStageAll(view)
      return ""
    except CatchableError:
      return "Failed to execute command gitui.stage-all: " & getCurrentExceptionMsg() & " " & getCurrentException().getStackTrace()

proc gitUiStageSelectedWrapper(args: string): string {.gcsafe.} =
  ## Stage selected file
  {.gcsafe.}:
    try:
      let args {.used.} = args.parseJsonexArgs()
      let view: GitUiView = getGitUiView()
      gitUiStageSelected(view)
      return ""
    except CatchableError:
      return "Failed to execute command gitui.stage-selected: " & getCurrentExceptionMsg() & " " & getCurrentException().getStackTrace()

proc gitUiUnstageSelectedWrapper(args: string): string {.gcsafe.} =
  ## Unstage selected file
  {.gcsafe.}:
    try:
      let args {.used.} = args.parseJsonexArgs()
      let view: GitUiView = getGitUiView()
      gitUiUnstageSelected(view)
      return ""
    except CatchableError:
      return "Failed to execute command gitui.unstage-selected: " & getCurrentExceptionMsg() & " " & getCurrentException().getStackTrace()

proc gitUiRevertSelectedWrapper(args: string): string {.gcsafe.} =
  ## Revert selected file
  {.gcsafe.}:
    try:
      let args {.used.} = args.parseJsonexArgs()
      let view: GitUiView = getGitUiView()
      gitUiRevertSelected(view)
      return ""
    except CatchableError:
      return "Failed to execute command gitui.revert-selected: " & getCurrentExceptionMsg() & " " & getCurrentException().getStackTrace()

proc gitUiDiffSelectedWrapper(args: string): string {.gcsafe.} =
  ## Diff selected file, commit or stash
  {.gcsafe.}:
    try:
      let args {.used.} = args.parseJsonexArgs()
      let view: GitUiView = getGitUiView()
      gitUiDiffSelected(view)
      return ""
    except CatchableError:
      return "Failed to execute command gitui.diff-selected: " & getCurrentExceptionMsg() & " " & getCurrentException().getStackTrace()

proc gitUiRefreshWrapper(args: string): string {.gcsafe.} =
  ## Refresh UI
  {.gcsafe.}:
    try:
      let args {.used.} = args.parseJsonexArgs()
      let view: GitUiView = getGitUiView()
      gitUiRefresh(view)
      return ""
    except CatchableError:
      return "Failed to execute command gitui.refresh: " & getCurrentExceptionMsg() & " " & getCurrentException().getStackTrace()

proc registerCommands(commands: CommandService) =
  const namespace = "gitui"
  discard commands.registerCommand(command_service.Command(
    namespace: namespace, name: "toggle", execute: gitUiToggleWrapper, active: false,
    description: """Toggle git ui""",))
  discard commands.registerCommand(command_service.Command(
    namespace: namespace, name: "cursor-down", execute: gitUiCursorDownWrapper, active: false,
    description: """Move cursor down""",))
  discard commands.registerCommand(command_service.Command(
    namespace: namespace, name: "cursor-up", execute: gitUiCursorUpWrapper, active: false,
    description: """Move cursor up""",))
  discard commands.registerCommand(command_service.Command(
    namespace: namespace, name: "push", execute: gitUiPushWrapper, active: false,
    description: """Push""",))
  discard commands.registerCommand(command_service.Command(
    namespace: namespace, name: "pull", execute: gitUiPullWrapper, active: false,
    description: """Pull""",))
  discard commands.registerCommand(command_service.Command(
    namespace: namespace, name: "stash", execute: gitUiStashWrapper, active: false,
    description: """Stash changes""",))
  discard commands.registerCommand(command_service.Command(
    namespace: namespace, name: "stash-pop", execute: gitUiStashPopWrapper, active: false,
    description: """Pop latest stash""",))
  discard commands.registerCommand(command_service.Command(
    namespace: namespace, name: "commit", execute: gitUiCommitWrapper, active: false,
    description: """Commit""",))
  discard commands.registerCommand(command_service.Command(
    namespace: namespace, name: "commit-amend", execute: gitUiCommitAmendWrapper, active: false,
    description: """Amend latest commit""",))
  discard commands.registerCommand(command_service.Command(
    namespace: namespace, name: "commit-edit-start", execute: gitUiCommitEditStartWrapper, active: false,
    description: """Start editing commit message""",))
  discard commands.registerCommand(command_service.Command(
    namespace: namespace, name: "commit-edit-cancel", execute: gitUiCommitEditCancelWrapper, active: false,
    description: """Cancel editing commit message""",))
  discard commands.registerCommand(command_service.Command(
    namespace: namespace, name: "commit-edit-confirm", execute: gitUiCommitEditConfirmWrapper, active: false,
    description: """Confirm commit message""",))
  discard commands.registerCommand(command_service.Command(
    namespace: namespace, name: "switch-branch", execute: gitUiSwitchBranchWrapper, active: false,
    description: """Switch branch""",))
  discard commands.registerCommand(command_service.Command(
    namespace: namespace, name: "fetch", execute: gitUiFetchWrapper, active: false,
    description: """Fetch""",))
  discard commands.registerCommand(command_service.Command(
    namespace: namespace, name: "reset-soft", execute: gitUiResetSoftWrapper, active: false,
    description: """Soft reset""",))
  discard commands.registerCommand(command_service.Command(
    namespace: namespace, name: "stage-all", execute: gitUiStageAllWrapper, active: false,
    description: """Stage all files""",))
  discard commands.registerCommand(command_service.Command(
    namespace: namespace, name: "stage-selected", execute: gitUiStageSelectedWrapper, active: false,
    description: """Stage selected file""",))
  discard commands.registerCommand(command_service.Command(
    namespace: namespace, name: "unstage-selected", execute: gitUiUnstageSelectedWrapper, active: false,
    description: """Unstage selected file""",))
  discard commands.registerCommand(command_service.Command(
    namespace: namespace, name: "revert-selected", execute: gitUiRevertSelectedWrapper, active: false,
    description: """Revert selected file""",))
  discard commands.registerCommand(command_service.Command(
    namespace: namespace, name: "diff-selected", execute: gitUiDiffSelectedWrapper, active: false,
    description: """Diff selected file, commit or stash""",))
  discard commands.registerCommand(command_service.Command(
    namespace: namespace, name: "refresh", execute: gitUiRefreshWrapper, active: false,
    description: """Refresh UI""",))
