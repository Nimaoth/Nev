# This file is auto generated by tools/command_generator.nim

import std/[options, json, tables]
import misc/[jsonex]
import scripting_api


proc profilerToggleWrapper(args: string): string {.gcsafe.} =
  ## Toggle Profiler UI
  {.gcsafe.}:
    try:
      let args {.used.} = args.parseJsonexArgs()
      let view: ProfilerView = getProfiler()
      profilerToggle(view)
      return ""
    except CatchableError:
      return "Failed to execute command profiler.toggle: " & getCurrentExceptionMsg() & " " & getCurrentException().getStackTrace()

proc profilerGraphWrapper(args: string): string {.gcsafe.} =
  ## Dump allocation graph for pointer argument
  {.gcsafe.}:
    try:
      let args {.used.} = args.parseJsonexArgs()
      let view: ProfilerView = getProfiler()
      profilerGraph(view, getArg[string](args.unnamed, args.named, 0, "arg", ""))
      return ""
    except CatchableError:
      return "Failed to execute command profiler.graph: " & getCurrentExceptionMsg() & " " & getCurrentException().getStackTrace()

proc registerCommands(commands: CommandService) =
  const namespace = "profiler"
  discard commands.registerCommand(command_service.Command(
    namespace: namespace, name: "toggle", execute: profilerToggleWrapper, active: false,
    description: """Toggle Profiler UI""",))
  discard commands.registerCommand(command_service.Command(
    namespace: namespace, name: "graph", execute: profilerGraphWrapper, active: false,
    description: """Dump allocation graph for pointer argument""",))
